[build]
  command = "npm install"
  functions = "functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["ytdl-core"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "18" 